DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/attendance
SECRET_KEY=change-me-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=GoGoFresh Attendance
WEBAUTHN_ORIGIN=http://localhost:3000
# Enable LAN access for local development (e.g. testing on phone via Wi-Fi).
# Remove or leave unset in production.
CORS_ORIGIN_REGEX=http://192\.168\.\d+\.\d+:3000
# NFC door-tap import shared secret (leave blank to disable /api/nfc/import)
NFC_IMPORT_API_KEY=change-me-in-production
